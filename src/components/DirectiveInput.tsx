import { LoaderCircle, Send } from 'lucide-react'
import { useState } from 'react'

type Props = { loading: boolean; disabled?: boolean; onSubmit: (directive: string) => Promise<boolean> }
export function DirectiveInput({ loading, disabled, onSubmit }: Props) { const [value, setValue] = useState(''); async function submit(event: React.FormEvent) { event.preventDefault(); if (!value.trim() || loading || disabled) return; if (await onSubmit(value.trim())) setValue('') } return <form onSubmit={(event) => void submit(event)} className="border-t border-zinc-800 p-4"><div className="flex gap-2"><input value={value} onChange={(event) => setValue(event.target.value)} disabled={loading || disabled} placeholder="Hoca şuna vurgu yaptı..." className="min-w-0 flex-1 border border-zinc-700 bg-zinc-900 px-3 py-2.5 text-sm text-zinc-200 outline-none placeholder:text-zinc-600 focus:border-emerald-400/60" /><button type="submit" disabled={!value.trim() || loading || disabled} title="Direktifi gönder" className="inline-flex shrink-0 items-center gap-2 bg-emerald-400 px-3 py-2 text-xs font-semibold text-zinc-950 disabled:cursor-not-allowed disabled:opacity-40">{loading ? <LoaderCircle size={15} className="animate-spin" /> : <Send size={15} />}Gönder</button></div></form> }
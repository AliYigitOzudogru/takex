import { convertFileSrc } from '@tauri-apps/api/core'
import type { Slide } from '../types/slides'
export function SlideThumbnailStrip({ slides, activeId, onSelect }: { slides: Slide[]; activeId?: string; onSelect: (slide: Slide) => void }) { return <div className="flex shrink-0 gap-2 overflow-x-auto border-t border-zinc-800 bg-zinc-950 p-3">{slides.map((slide, index) => <button type="button" key={slide.id} onClick={() => onSelect(slide)} className={`relative h-16 w-24 shrink-0 overflow-hidden border ${activeId === slide.id ? 'border-emerald-400' : 'border-zinc-800 hover:border-zinc-600'}`}><img src={convertFileSrc(slide.imagePath)} alt={`Slayt ${index + 1}`} className="h-full w-full object-cover" /><span className="absolute bottom-0 left-0 bg-black/70 px-1.5 py-0.5 text-[10px] text-zinc-200">{index + 1}</span></button>)}</div> }